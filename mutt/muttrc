source "/home/samuel/local/src/mutt-colors-solarized/mutt-colors-solarized-dark-16.muttrc"
set imap_user = $MUTT_GMAIL_USER
set imap_pass = $MUTT_GMAIL_PASS
set spoolfile = "imaps://imap.gmail.com:993/INBOX"
set folder = "imaps://imap.gmail.com:993"

# We don't want to record as the SMTP server does it for us already
# set record="+[Google Gmail]/Sent Mail"
set postponed="+[Google Mail]/Drafts"
set header_cache="~/.mutt/cache/headers"
set message_cachedir="~/.mutt/cache/bodies"
set certificate_file="~/.mutt/certificates"
set sendmail = "/usr/bin/msmtp"

# Personal taste
# --------------
set copy = no
set sort=threads
# set imap_check_subscribed
